<section id="hero" class="hero">
    <div class="hero-bg"></div>
    <div class="section-container hero-container">
        <div class="hero-content">
            <span class="hero-eyebrow">Profesjonalne zarządzanie najmem · Kraków</span>
            <h1 class="hero-title">Twój czynsz na koncie.<br/><em class="hero-title-em">Bez Twojego czasu.</em></h1>
            <div class="hero-body">
                <p class="hero-subtitle">
                    Przejmujemy wynajem Twojego mieszkania od&nbsp;A&nbsp;do&nbsp;Z — od przygotowania lokalu, przez znalezienie najemców, po obsługę i&nbsp;bieżące sprawy.
                </p>
                <p class="hero-subtitle">
                    Ty otrzymujesz stały czynsz, bez angażu z&nbsp;Twojej strony.
                </p>
                <p class="hero-hook">Sam zdecydujesz, czy to dla Ciebie.</p>
                <div class="hero-cta">
                    <a href="tel:+48510159125" class="btn btn--primary btn--big">
                        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.627 0-12 5.373-12 12s5.373 12 12 12 12-5.373 12-12-5.373-12-12-12zm3.445 17.827c-3.684 1.684-9.401-9.43-5.8-11.308l1.053-.519 1.746 3.409-1.042.513c-1.095.587 1.185 5.04 2.305 4.497l1.032-.505 1.76 3.397-1.054.516z"/></svg>
                        Zadzwoń: 510 159 125
                    </a>
                    <a href="#contact" class="btn btn--outline">Napisz do nas</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        padding-block: 7rem var(--px-64);
        display: flex;
        align-items: center;
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        background-image: url('/images/reja.jpg');
        background-size: cover;
        background-position: center;
        z-index: -1;
    }

    .hero-bg::after {
        content: '';
        position: absolute;
        inset: 0;
        background:
            linear-gradient(
                180deg,
                rgba(255, 255, 255, 0.55) 0%,
                rgba(255, 255, 255, 0.2) 18%,
                rgba(255, 255, 255, 0.2) 100%
            );
    }

    .hero-container {
        width: 100%;
        display: flex;
        justify-content: center;
    }

    .hero-content {
        max-width: 52rem;
        text-align: center;
    }

    .hero-eyebrow {
        display: inline-block;
        margin-bottom: var(--px-20);
        padding: var(--px-6) var(--px-14);
        background: rgba(255, 255, 255, 0.6);
        -webkit-backdrop-filter: blur(6px);
        backdrop-filter: blur(6px);
        border: 1px solid rgba(184, 134, 11, 0.4);
        border-radius: 999px;
        color: var(--color-accent);
        font-size: var(--text-xs);
        font-weight: 600;
        letter-spacing: 0.14em;
        text-transform: uppercase;
    }

    .hero-title {
        margin-bottom: var(--px-40);
        font-size: clamp(3rem, 7vw, 5rem);
        font-weight: 400;
        line-height: 1.12;
        color: #000;
        letter-spacing: -0.02em;
    }

    .hero-title-em {
        font-style: italic;
        font-weight: 400;
    }

    .hero-body {
        background: rgba(255, 255, 255, 0.25);
        -webkit-backdrop-filter: blur(4px);
        backdrop-filter: blur(4px);
        border-radius: var(--radius-lg);
        padding: var(--px-36) var(--px-48);
    }

    .hero-subtitle {
        font-size: clamp(1.1rem, 2vw, 1.35rem);
        margin-bottom: var(--px-14);
        line-height: 1.85;
        color: #000;
        font-weight: 400;
        letter-spacing: 0.01em;
    }

    .hero-hook {
        font-size: clamp(1.25rem, 2.2vw, 1.5rem);
        font-weight: 600;
        margin-top: var(--px-16);
        margin-bottom: var(--px-40);
        color: #000;
        letter-spacing: 0.01em;
    }

    .hero-cta {
        display: flex;
        gap: var(--px-20);
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
    }

    .btn--big {
        padding: var(--px-20) var(--px-40);
        font-size: var(--text-base);
        display: inline-flex;
        align-items: center;
        gap: var(--px-10);
    }

    .btn--big svg {
        flex-shrink: 0;
    }

    .btn--outline {
        padding: var(--px-20) var(--px-40);
        font-size: var(--text-base);
        border: 2px solid var(--color-primary);
        color: var(--color-primary);
        background: rgba(255, 255, 255, 0.7);
        -webkit-backdrop-filter: blur(6px);
        backdrop-filter: blur(6px);
        border-radius: var(--px-4);
        font-weight: 600;
        letter-spacing: 0.03em;
        transition: all var(--transition-short);
    }

    @media (hover: hover) and (pointer: fine) {
        .btn--outline:hover {
            background-color: var(--color-primary);
            color: var(--color-white);
        }
    }

    @media screen and (max-width: 48rem) {
        .hero {
            min-height: auto;
            padding-block: 8rem var(--px-56);
        }

        .hero-content {
            text-align: left;
        }

        .hero-cta {
            justify-content: flex-start;
        }

        .hero-title {
            font-size: clamp(2.25rem, 9vw, 3.5rem);
        }
    }
</style>
