<section id="hero" class="hero">
    <div class="hero-bg"></div>
    <div class="section-container hero-container">
        <div class="hero-content">
            <h1 class="hero-title">Twój czynsz na koncie.<br/>Bez Twojego czasu.</h1>
            <div class="hero-body">
                <p class="hero-subtitle">
                    Przejmujemy wynajem Twojego mieszkania od&nbsp;A&nbsp;do&nbsp;Z — od przygotowania lokalu, przez znalezienie najemców, po obsługę i&nbsp;bieżące sprawy.
                </p>
                <p class="hero-subtitle">
                    Ty otrzymujesz stały czynsz, bez angażu z&nbsp;Twojej strony.
                </p>
                <p class="hero-hook">Zadzwoń i&nbsp;sam zdecydujesz, czy to dla Ciebie.</p>
                <div class="hero-cta">
                    <a href="tel:+48510159125" class="btn btn--primary btn--big">
                        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.627 0-12 5.373-12 12s5.373 12 12 12 12-5.373 12-12-5.373-12-12-12zm3.445 17.827c-3.684 1.684-9.401-9.43-5.8-11.308l1.053-.519 1.746 3.409-1.042.513c-1.095.587 1.185 5.04 2.305 4.497l1.032-.505 1.76 3.397-1.054.516z"/></svg>
                        510 159 125
                    </a>
                    <a href="#contact" class="btn btn--outline">Napisz do nas</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        padding-block: 6rem var(--px-64);
        display: flex;
        align-items: center;
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        background-image: url('/images/reja.jpg');
        background-size: cover;
        background-position: center;
        z-index: -1;
    }

    .hero-bg::after {
        content: '';
        position: absolute;
        inset: 0;
        background:
            linear-gradient(
                180deg,
                rgba(255, 255, 255, 0.55) 0%,
                rgba(255, 255, 255, 0.2) 18%,
                rgba(255, 255, 255, 0.2) 100%
            );
    }

    .hero-container {
        width: 100%;
        display: flex;
        justify-content: center;
    }

    .hero-content {
        max-width: 52rem;
        text-align: center;
    }

    .hero-title {
        margin-bottom: var(--px-24);
        font-size: clamp(3rem, 7vw, 5rem);
        font-weight: 400;
        line-height: 1.12;
        color: #000;
        letter-spacing: -0.02em;
    }

    .hero-body {
        background: rgba(255, 255, 255, 0.35);
        -webkit-backdrop-filter: blur(18px) saturate(1.4);
        backdrop-filter: blur(18px) saturate(1.4);
        border: 1px solid rgba(255, 255, 255, 0.5);
        border-radius: var(--radius-lg);
        padding: var(--px-40) var(--px-56);
        text-align: center;
        box-shadow:
            0 8px 32px rgba(45, 59, 45, 0.08),
            inset 0 1px 0 rgba(255, 255, 255, 0.6);
    }

    .hero-subtitle {
        font-size: clamp(1.05rem, 1.5vw, 1.2rem);
        margin-inline: auto;
        margin-bottom: var(--px-16);
        max-width: 40rem;
        line-height: 1.7;
        color: #1a1a1a;
        font-weight: 500;
        letter-spacing: 0.005em;
        text-align: center;
        text-wrap: balance;
    }

    .hero-subtitle:last-of-type {
        margin-bottom: 0;
    }

    .hero-hook {
        text-align: center;
    }

    .hero-cta {
        justify-content: center;
    }

    .hero-hook {
        font-size: clamp(1.25rem, 2.2vw, 1.5rem);
        font-weight: 600;
        margin-top: var(--px-16);
        margin-bottom: var(--px-40);
        color: #000;
        letter-spacing: 0.01em;
    }


    .hero-cta {
        display: flex;
        gap: var(--px-20);
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
    }

    .btn--big {
        padding: var(--px-20) var(--px-40);
        font-size: var(--text-base);
        display: inline-flex;
        align-items: center;
        gap: var(--px-10);
    }

    .btn--big svg {
        flex-shrink: 0;
    }

    .btn--outline {
        padding: var(--px-20) var(--px-40);
        font-size: var(--text-base);
        border: 2px solid var(--color-primary);
        color: var(--color-primary);
        background: rgba(255, 255, 255, 0.7);
        -webkit-backdrop-filter: blur(6px);
        backdrop-filter: blur(6px);
        border-radius: var(--px-4);
        font-weight: 600;
        letter-spacing: 0.03em;
        transition: all var(--transition-short);
    }

    @media (hover: hover) and (pointer: fine) {
        .btn--outline:hover {
            background-color: var(--color-primary);
            color: var(--color-white);
        }
    }

    @media screen and (max-width: 48rem) {
        .hero {
            min-height: auto;
            padding-block: 6rem var(--px-48);
            padding-inline: var(--px-16);
        }

        .hero-body {
            padding: var(--px-24) var(--px-20);
        }

        .hero-title {
            font-size: clamp(2rem, 8vw, 3rem);
            margin-bottom: var(--px-20);
        }

        .hero-subtitle {
            font-size: var(--text-base);
            line-height: 1.6;
            margin-bottom: var(--px-12);
        }

        .hero-hook {
            font-size: var(--text-lg);
            margin-top: var(--px-12);
            margin-bottom: var(--px-24);
        }

        .hero-cta {
            justify-content: stretch;
            flex-direction: column;
            gap: var(--px-12);
        }

        .hero-cta .btn {
            width: 100%;
            justify-content: center;
        }

        .btn--big,
        .btn--outline {
            padding: var(--px-16) var(--px-24);
        }
    }
</style>
